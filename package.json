{
  "name": "winterswap",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate-dev-reset": "truffle migrate --reset --network development",
    "migrate-huobi_test-reset": "truffle migrate --reset --skip-dry-run --network huobi_test",
    "compile": "truffle compile --all",
    "test": "truffle test --network development"
  },
  "author": "Winterman",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/test-helpers": "^0.5.9",
    "@truffle/hdwallet-provider": "1.1.0",
    "truffle": "5.1.56"
  }
}
